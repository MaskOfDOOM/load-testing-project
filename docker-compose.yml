services:
  statsd-exporter:
    image: prom/statsd-exporter:v0.26.1
    container_name: load-testing-statsd-exporter
    restart: unless-stopped
    ports:
      - "9125:9125"  # Порт для приёма метрик StatsD
      - "9102:9102"  # Порт для Prometheus (HTTP)
    command:
      - '--statsd.mapping-config=/etc/statsd/mapping.yml'
    volumes:
      - ./prometheus/statsd-mapping.yml:/etc/statsd/mapping.yml:ro
    networks:
      - load-testing-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9102/metrics"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'

  prometheus:
    image: prom/prometheus:v3.0.1
    container_name: load-testing-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=7d'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - load-testing-network
    depends_on:
      - statsd-exporter
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '0.5'

  grafana:
    image: grafana/grafana:11.5.0
    container_name: load-testing-grafana
    restart: unless-stopped
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GF_SECURITY_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD:-admin123}
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/grafana.ini:/etc/grafana/grafana.ini:ro
    networks:
      - load-testing-network
    depends_on:
      - prometheus
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'

  k6:
    image: grafana/k6:0.50.0
    container_name: load-testing-k6
    # k6 — инструмент для запуска тестов, а не сервис, поэтому restart не нужен
    volumes:
      - ./k6:/scripts:ro
      - ./results:/results
      - ./config.yaml:/config.yaml:ro
    networks:
      - load-testing-network
    depends_on:
      - statsd-exporter
    entrypoint: ["/bin/sh"]
    command: ["-c", "tail -f /dev/null"]
    stdin_open: true
    tty: true

networks:
  load-testing-network:
    driver: bridge

volumes:
  prometheus-data:
  grafana-data:
